/**
 * AppShell - Template
 * One-line description.
 * Used in: TBD
 */
import React from 'react';
import type { AppShellProps } from './AppShell.types';

export const AppShell: React.FC<AppShellProps> = (props) => {
  return (
    <div className="p-4" data-testid="AppShell">
      AppShell Component
    </div>
  );
};
