/**
 * Select - Atom
 * One-line description.
 * Used in: TBD
 */
import React from 'react';
import type { SelectProps } from './Select.types';

export const Select: React.FC<SelectProps> = (props) => {
  return (
    <div className="p-4" data-testid="Select">
      Select Component
    </div>
  );
};
