/**
 * Icon - Atom
 * One-line description.
 * Used in: TBD
 */
import React from 'react';
import type { IconProps } from './Icon.types';

export const Icon: React.FC<IconProps> = (props) => {
  return (
    <div className="p-4" data-testid="Icon">
      Icon Component
    </div>
  );
};
